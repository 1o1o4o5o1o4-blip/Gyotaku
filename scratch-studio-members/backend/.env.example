# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (Base64 or Local)
DB_TYPE=local
DB_PATH=./data/studios.db

# Scratch API
SCRATCH_API_BASE=https://api.scratch.mit.edu

# Batch Job
BATCH_CRON=0 0 * * *
